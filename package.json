{
  "name": "plan-facttic",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && touch docs/.nojekyll && git add docs/ && git commit -m 'deploy' && git push origin main"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "gh-pages": "^6.3.0",
    "tailwindcss": "^4.2.2",
    "vite": "^8.0.4"
  }
}
